---
import Login from "~/components/widgets/Login.astro";
// import { getTranslations, t } from "../../../i18n";
// const metadata = {
//   title: 'Envyz Me  — Free template for creating websites with Astro + Tailwind CSS',
//   ignoreTitleTemplate: true,
// };

export function getStaticPaths() {
  return [
    { params: { locale: "en" } },
    { params: { locale: "fr" } },
    { params: { locale: "th" } },                                                    
    { params: { locale: "la" } },
  ];
}

// const { locale } = Astro.params;
---
<div>
    <Login/>
</div>